import React from "react";
import "./App.css";
import { Modal } from "shared/ui";

const App: React.FC = () => {
    return (
        <div>
            <Modal />
        </div>
    );
};

export default App;
