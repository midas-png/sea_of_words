import React from "react";
import "./App.css";
import { Button } from "./shared/ui";

const App: React.FC = () => {
    return (
        <div>
            <Button>Следующий</Button>
        </div>
    );
};

export default App;
